services:
  - type: web
    name: technoglobe-certificates
    env: python
    buildCommand: "pip install -r requirements.txt && cd client && npm install && npm run build && cd .."
    startCommand: "python -m uvicorn server.main:app --host 0.0.0.0 --port $PORT"
    plan: free
